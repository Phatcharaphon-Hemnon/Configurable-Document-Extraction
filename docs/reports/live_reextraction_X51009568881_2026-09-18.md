# Live re-extraction of X51009568881.jpg under acceptance policy v1.5.0

Date: 2026-09-18. Policy: `ACCEPTANCE_POLICY_VERSION` **v1.5.0** (working tree,
uncommitted — same code as `docs/reports/acceptance_totals_placeholder_2026-09-18.md`).
Prior jobs on this file: `ef519c25` (policy v1.3.0, wrong `total_amount="40.40"`
from CASH line, completeness 0.25) → `6e97cb52` (v1.4.0, 0 fields, generic
structural message) → `ae4caa84` (v1.5.0, 0 fields, specific placeholder message).

## Dispatch (exactly ONE, no retries)

- New dispatch, normal path (no `force_refresh`, no `doc_type` override — cache
  invalidation is what is under test): `POST /api/extract` with byte-identical
  file (`sha256 d7bfcb8d…6c46`, 289,995 bytes, `X51009568881.jpg`).
- Returned `job_id 6fb6ba5c-db55-41c8-9021-fa6da9d29ac1` (`queued` → `processing`
  → `completed` in ~30s of polling; attempt bound 300s, no timeout, dispatch
  count 1/3).
- Caps honored: 300s max attempt / 600s max doc / 660s overall / ≤3 dispatches /
  no retries / stop-after-first-timeout (never triggered).
- Idleness pre-established: API `health ok`, 0 non-terminal jobs in
  `data-local/extraction.db`, LLM reachable
  (`scb10x/llama3.2-typhoon2-3b-instruct` present), worker freshly reloaded
  (child spawned 09:25:12 after the 09:25:13 acceptance edits — serves v1.5.0;
  confirmed post-hoc by `acceptance_policy_version: v1.5.0` on the result).

## Cache-miss confirmation (not assumed)

- Pre-dispatch: `result_cache` 0 entries, `result_manifest` 0 rows; on-disk code
  has `ACCEPTANCE_POLICY_VERSION = "v1.5.0"` while prior jobs carry v1.3.0 /
  v1.4.0 / v1.5.0 — fingerprint covers the version, so stale entries miss by
  construction.
- Post-dispatch miss signals (all four): `cache_metadata: None`, no
  `result_cache_hit` in timings, real LLM timings (`router 1.30s`,
  `extractor 13.67s`), new `source_id 67d969ca-…` + `extracted_at
  2026-09-18T02:30:19Z` (= dispatch time).
- OCR text byte-identical to all three prior runs (deterministic OCR; the fix
  scope is acceptance-layer, as claimed).

## Findings vs the five questions

1. **`total_amount`: still missing — extractor proposed no total at all.**
   Rejected candidates contain no `total_amount` entry, so neither the new
   CASH-line rejection nor the Total-line accept path was exercised live. The
   `Missing required field: total_amount` validation error is a downstream
   consequence of model recall (this run's extractor emitted only
   `invoice_number` + one `line_items` row — same minimal shape as `ae4caa84`),
   not of the new check rejecting a good value. Unit level stays green
   (`test_total_grounded_to_cash_line_rejected`,
   `test_total_bare_span_on_total_line_accepted`, 18/18 in
   `test_acceptance.py`): had the model proposed `40.40` on the CASH line it
   would now be rejected naming that line; `10.40` on the Total line would be
   accepted. The `ef519c25`-style wrong acceptance cannot recur under v1.5.0
   for this page shape.
2. **`line_items` row: still fails structurally — as expected, message improved.**
   `structurally invalid: placeholder column key (row keys ['...', '...',
   '...'] vs declared ['description', 'qty', 'unit_price', 'total'])` + `table
   withheld: no fully grounded rows`. Same rejection as `6e97cb52`, now naming
   the placeholder mode (the v1.5.0 change, confirmed live).
3. **`invoice_number`: still correctly rejected as hallucination — untouched.**
   `'1126679'`, `source_span not found in document (possible hallucination)`,
   identical value/reason across all four runs (`ef519c25`, `6e97cb52`,
   `ae4caa84`, `6fb6ba5c`). The v1.5.0 diff does not touch the short-span
   verbatim rule — confirmed unaffected.
4. **Overall: `needs_review True`, `completeness_score 0.0`, 0 accepted fields,
   `acceptance unresolved`, `judge flagged 0.0` ("No extracted values to
   review.")** — exactly the screenshot baseline (0.0 / 0/0) and identical to
   `ae4caa84`. No regression, no recall gain on this doc (bottleneck is the
   3B extractor proposing almost nothing, not acceptance).
5. **Persistence + measurement fidelity: PASS with two caveats.**
   - PASS: job + page rows in `data-local/extraction.db` (5 jobs total, new row
     `completed/invoice/0.0/needs_review`), source dir
     `data-local/sources/67d969ca-…/` with `original` (JPEG 884×1678) +
     `page-1.png` + `metadata.json`; all reported numbers are real
     (`pipeline 16.00s`, validator 0.007s, judge 0.0s skipped-empty).
   - Caveat A: `timings.ocr=0.0 + ocr_cached=1.0` with NO new disk OCR entry
     (dir still holds only the unrelated MPH file) in a freshly reloaded
     worker — the hit must come from inside this job's own pipeline
     (pre-pass populates memory, streaming path hits it with seconds zeroed).
     `ocr=0.0` therefore understates OCR work; `full_text` identity bounds the
     impact to timing attribution only.
   - Caveat B: `result_cache` still holds 0 entries after this cacheable run
     (`flagged`, no error/`failed_stage` → eligible per `is_cacheable_result`).
     The `put` path (extraction_service.py:688) either silently skipped or
     raised into its bare `except: pass`. Worth one follow-up look; does not
     affect this verification (miss direction is what was required).

## Verification checks run

- `python -m pytest api/tests/test_acceptance.py -q` → **18 passed** (0.52s).
- `ruff check api/` → **All checks passed**.
- Full backend suite → **BLOCKED** (overall 660s budget reserved for the live
  dispatch + analysis; prior report documents its pre-existing state: 9
  collection errors + 28 failures in region-pipeline/HTTP-budget/streaming
  tests, reproduced with these changes stashed).
- Frontend build → not run (no UI changes).

## Workflows consulted vs actually run

- Consulted: `.opencode/ecc/commands/ecc-verify.md` (procedure/table format),
  `.opencode/ecc/commands/ecc-update-docs.md` (this file), prior report
  `docs/reports/acceptance_totals_placeholder_2026-09-18.md` (baseline).
- Actually run: focused pytest + ruff + ONE live `POST /api/extract` dispatch
  with poll loop + read-only sqlite/API inspection. No restarts, no code
  changes, no pushes.

## Rollback

No changes made by this verification (working tree untouched) — nothing to
roll back. The v1.5.0 fix itself remains uncommitted working-tree state.
