Runtime storage moved to the repository-root `data/` directory.

Both histories were merged without dropping jobs. Legacy backups are in
`data/backups/`. See `docs/page_storage.md` for migration and rollback.
