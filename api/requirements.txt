fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
python-dotenv>=1.0
python-multipart>=0.0.9
openai>=1.40
temporalio>=1.7
langfuse>=4.0
# Local OCR (on-host, no API key, no network).
# NOTE: small ONNX models (~15MB) auto-download on first run, then cached.
rapidocr_onnxruntime==1.2.3
pymupdf>=1.24
pillow>=10.0
numpy>=1.26
